const colors = {
  pointViolet: '#5c49a7',
};

const fontSizes = {
  s: '12px',
  sm: '14px',
  md: '16px',
  lg: '18px',
  xl: '20px',
  xxl: '24px',
  xxxl: '28px',
  xxxxl: '32px',
};

const fontWeights = {
  normal: 400,
  bold: 700,
};

const theme = { colors, fontSizes, fontWeights };

export default theme;
